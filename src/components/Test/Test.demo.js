import React from "react";

/**
 * Imports other components and hooks.
 */
import Test from ".";

/**
 * Displays the Test demo.
 */
const TestDemo = (props) => {
  return <Test />;
};

export default TestDemo;
